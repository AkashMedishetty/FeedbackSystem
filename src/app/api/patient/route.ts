import { NextResponse } from 'next/server';

export async function GET() {
  return NextResponse.json({ 
    message: 'Patient endpoints will be implemented in subsequent tasks' 
  });
}

export async function POST() {
  return NextResponse.json({ 
    message: 'Patient endpoints will be implemented in subsequent tasks' 
  });
}